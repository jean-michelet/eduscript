{
  "name": "eduscript",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "test": "jest src",
    "watch": "npm run clean && tsc --watch",
    "build": "npm run clean && tsc",
    "clean": "rm -rf dist",
    "standard": "ts-standard --fix",
    "standard-check": "ts-standard"
  },
  "devDependencies": {
    "@types/jest": "^29.5.4",
    "@types/node": "^20.6.0",
    "jest": "^29.7.0",
    "jest-extended": "^4.0.2",
    "ts-jest": "^29.1.1",
    "ts-standard": "^12.0.2",
    "typescript": "^5.2.2"
  }
}
